stages:
  - test

install-dependencies:
  stage: test
  image: golang:1.19-alpine
  script:
    - go mod download
    - go mod verify

golang-test:
  stage: test
  image: golang:1.19-alpine
  script:
    - go install gotest.tools/gotestsum@latest
    - gotestsum cgo_enabled=0 --junitfile report.xml --format testname ./services.boilerplate/... ./services.shared/...
  artifacts:
    when: always
    reports:
      junit: report.xml